/**
 * DEPRECATED: This hook is replaced by next-themes to ensure consistent
 * context across shadcn/ui components and the rest of the app.
 * Use `import { useTheme } from 'next-themes'` instead.
 */
export { useTheme } from 'next-themes';